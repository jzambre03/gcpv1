# =============================================================================
# Golden Config AI Multi-Agent System - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# AWS Configuration (Required)
# -----------------------------------------------------------------------------
# AWS region where Bedrock is available
AWS_REGION=us-east-1

# AWS credentials (Required for local testing, optional on EC2 with IAM role)
# On EC2 with IAM role, these are automatically provided
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# -----------------------------------------------------------------------------
# AWS Bedrock Configuration (Required)
# -----------------------------------------------------------------------------
# Supervisor uses Claude Sonnet for complex orchestration
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# Workers use Claude Haiku for faster, cost-effective processing
BEDROCK_WORKER_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0

# Optional: Bedrock Guardrails (leave empty if not using)
BEDROCK_GUARDRAILS_ID=
BEDROCK_GUARDRAILS_VERSION=DRAFT

# -----------------------------------------------------------------------------
# Server Configuration (Optional - has defaults)
# -----------------------------------------------------------------------------
# Host to bind the FastAPI server to
# 0.0.0.0 = all interfaces (accessible from other machines)
# 127.0.0.1 = localhost only (more secure for local dev)
HOST=0.0.0.0

# Port for the FastAPI server
PORT=3000

# Base URL for generating links (used in reports and notifications)
# Update this if deploying behind a proxy or on a different domain
BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Agent Configuration (Optional - has defaults)
# -----------------------------------------------------------------------------
# Supervisor agent identifier
SUPERVISOR_AGENT_ID=supervisor-agent

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Runtime mode (development, production)
AGENT_RUNTIME_MODE=development

# -----------------------------------------------------------------------------
# Git Configuration (Required for private repositories)
# -----------------------------------------------------------------------------
# GitLab personal access token (required for private repos)
# Generate at: https://your-gitlab.com/-/profile/personal_access_tokens
# Scopes needed: read_repository, write_repository
GITLAB_TOKEN=

# Git user info for commits (optional - used when creating branches)
GIT_USER_NAME=Golden Config AI
GIT_USER_EMAIL=golden-config-ai@your-company.com

# -----------------------------------------------------------------------------
# Repository Configuration (Optional - configured in main.py)
# -----------------------------------------------------------------------------
# Legacy default repo URL (each service has its own URL in SERVICES_CONFIG)
# This is only used for backward compatibility with legacy endpoints
DEFAULT_REPO_URL=https://gitlab.example.com/org/golden_config.git

# Default branch names (optional)
DEFAULT_GOLDEN_BRANCH=gold
DEFAULT_DRIFT_BRANCH=drift
DEFAULT_MAIN_BRANCH=main
DEFAULT_ENVIRONMENT=prod

# =============================================================================
# EC2 DEPLOYMENT NOTES
# =============================================================================
# When deploying on EC2 with IAM role, you only need:
#   - AWS_REGION
#   - BEDROCK_MODEL_ID
#   - BEDROCK_WORKER_MODEL_ID
#   - GITLAB_TOKEN (if using private repos)
#   - HOST (optional, defaults to 0.0.0.0)
#   - PORT (optional, defaults to 3000)
#   - BASE_URL (update to your EC2 public URL/domain)
#
# AWS credentials are automatically provided by the IAM role.
#
# Example EC2 BASE_URL:
#   BASE_URL=http://ec2-xx-xxx-xxx-xx.compute-1.amazonaws.com:3000
#   Or with a domain:
#   BASE_URL=https://golden-config.your-company.com
#
# Required IAM permissions:
#   - bedrock:InvokeModel
#   - bedrock:InvokeModelWithResponseStream
# =============================================================================
